---
galaxy_info:
  author: ansible-env-bootstrap
  description: Ensure QEMU and UTM (macOS cask) are installed via Homebrew on macOS or QEMU via pacman on Arch Linux.
  license: MIT
  min_ansible_version: '2.13'
  platforms:
    - name: MacOSX
      versions: [ 'all' ]
    - name: GenericLinux
      versions: [ 'all' ]
dependencies:
  - role: homebrew
    when: (ansible_facts['os_family'] | default('')) == 'Darwin' and (device_type | default('')) == 'macbook'
  - role: aur-helper
    when: >-
      (ansible_facts['os_family'] | default('')) == 'Archlinux' or
      (ansible_facts['distribution'] | default('')) == 'Archlinux' or
      (distro | default('')) == 'archlinux'
