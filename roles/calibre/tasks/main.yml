---
# tasks file for calibre (install via Homebrew Cask on macOS)

- name: Determine allowed platform for Calibre setup (macOS MacBook only)
  ansible.builtin.set_fact:
    calibre_macos_allowed: >-
      {{ (ansible_facts['os_family'] | default('')) == 'Darwin' and (device_type | default('')) == 'macbook' }}

- name: Stop role when not on a supported platform (macOS MacBook)
  ansible.builtin.meta: end_role
  when: not (calibre_macos_allowed | bool)

- name: Conditional block for calibre

  when: calibre_macos_allowed | bool

  block:
    - name: Ensure Calibre is present via Homebrew Cask (macOS)
      community.general.homebrew_cask:
        name: calibre
        state: present
      become: false
